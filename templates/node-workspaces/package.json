{
  "private": true,
  "scripts": {
    "build": "pnpm -r --stream --filter=@tomjs/* build",
    "test": "vitest",
    "lint:eslint": "eslint \"packages/**/*.ts\" *.{js,cjs,ts} --fix --cache"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "tsx": "^4.11.0",
    "type-fest": "^4.18.3",
    "vitest": "^1.6.0"
  }
}
